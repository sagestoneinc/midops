# SwissCRM API — copy this to .env and fill in values

# Google BigQuery
BQ_PROJECT=your-gcp-project-id
BQ_DATASET=your_bigquery_dataset
GCP_CREDENTIALS_PATH=/absolute/path/to/service-account.json

# API key that clients send in the X-API-Key header
SWISS_CRM_API_KEY=replace-with-your-api-key
